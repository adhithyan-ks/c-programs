#include<stdio.h>

int main() {
    int i,size,arr[10];
    printf("Enter size of array:");
    scanf("%d",&size);
    printf("Enter array elements:");
    for(i=0;i<n;i++) {
        scanf("%d",&arr[i]);
    }
}